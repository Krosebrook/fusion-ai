/**
 * Role entity wrapper
 * 
 * This module exports the Role entity for managing user roles.
 */
import { base44 } from '../api/base44Client';

/**
 * Role entity for creating, reading, updating, and deleting roles.
 */
export const Role = base44.entities.Role;
