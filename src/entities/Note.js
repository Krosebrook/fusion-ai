/**
 * Note entity wrapper
 * 
 * This module exports the Note entity for managing notes.
 */
import { base44 } from '../api/base44Client';

/**
 * Note entity for creating, reading, updating, and deleting notes.
 */
export const Note = base44.entities.Note;
